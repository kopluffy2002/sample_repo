server {
    listen 8080;

    # ── Serve Angular frontend ────────────────────────────────────────────────
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # ── Proxy API requests to backend Cloud Run service ───────────────────────
    location /api/ {
        proxy_pass https://schedumingle-backend-516427689972.asia-south1.run.app/;
        proxy_http_version 1.1;
        proxy_set_header Host schedumingle-backend-516427689972.asia-south1.run.app;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_ssl_server_name on;
    }

    # ── Proxy WebSocket (socket.io) to backend Cloud Run service ─────────────
    location /socket.io/ {
        proxy_pass https://schedumingle-backend-516427689972.asia-south1.run.app/socket.io/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host schedumingle-backend-516427689972.asia-south1.run.app;
        proxy_ssl_server_name on;
    }

    # ── Compression ───────────────────────────────────────────────────────────
    gzip on;
    gzip_types text/plain text/css application/json application/javascript
               text/xml application/xml application/javascript;
    gzip_min_length 1000;
}